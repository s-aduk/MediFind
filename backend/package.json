{
  "name": "medifind-backend",
  "version": "1.0.0",
  "description": "Backend Lambda functions for MediFind",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:unit": "jest --testPathPattern=__tests__",
    "test:integration": "jest --testPathPattern=integration",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix"
  },
  "workspaces": [
    "src/functions/*",
    "src/common"
  ],
  "devDependencies": {
    "jest": "^29.7.0",
    "eslint": "^8.57.0",
    "babel-jest": "^29.7.0",
    "@babel/preset-env": "^7.24.0"
  }
}